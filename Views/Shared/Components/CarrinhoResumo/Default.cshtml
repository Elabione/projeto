@model CarrinhoViewModel
@if (Model.Carrinho.CarrinhoItens.Count > 0)
{
    <a asp-controller="Carrinho" asp-action="Index" class="nav-link">
      <i class="bi bi-cart2"></i> @Model.Carrinho.CarrinhoItens.Count
    </a>
}