
<div class="col">
        <div class="card mb-3 rounded-right border-dark">
            <div class="row g-0">
                <div class="col-5 col-md-3">
                    <img src="@Model.ImagemPequenaUrl" class="img-fluid-rounded-start h-100 w-100" alt="...">
                </div>
                <div class="col-7 col-md-9">
                    <div class="card-body">
                        <h6 class="card-title ms-5"><a class="link-dark" style="font-size: 200%" asp-controller="Item" asp-action="Details" asp-route-itemId="@Model.ItemId">@Model.Nome</a></h6>
                        <h6 class="card-text ms-5">@Model.DescricaoCurta</h6>
                        <h5 class="ms-5">Preço : @Model.Preco.ToString("c")</h5>
                        <a class="btn btn-primary rounded-pill text-white pt-2 ps-4 pe-4 mb-2 mt-3 ms-5 marrom" id="cartButton" asp-controller="Carrinho" asp-action="AdicionarItemNoCarrinhoCompra" asp-route-itemId ="@Model.ItemId"><h5>+ Adicionar</h5></a>
                    </div>
                </div>
            </div>
        </div>
</div>